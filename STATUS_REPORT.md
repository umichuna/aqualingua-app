# AquaLingua 状況報告書

このファイル1つで、経緯・修正履歴・原因・未対応項目・次回作業・変更ファイル・
必要な技術情報を把握できるようにする。過去のチャットログを遡る必要はない。
**最新の対応が一番上。**

---

## 2026-09-06（追記14）: クラウド保存が 413 で失敗するバグを修正（背景画像の肥大化）

### 症状（オーナー報告）
「クラウド保存が失敗する」。魚の大きさランクの変更など、他の操作は正常だった。

### 原因
本番ログに `POST /api/sync/push` → **HTTP 413（送信データが大きすぎる）** が記録されていた。
Vercel のリクエスト本文の上限は **4.5MB** で、これを超えると本文がサーバーに届かないまま
弾かれる。そのためアプリ側からは「原因不明の失敗」に見えていた。

DBを実測した結果、`user_status` の1レコードが突出して大きかった。

| 中身 | サイズ |
|---|---|
| 水槽の背景画像（`tanks`） | **3.02 MB**（うち「テスト淡水」1枚で 2.41 MB） |
| カスタム魚の画像（`customFish`） | 0.35 MB |
| 単語・記録などその他すべて | 0.03 MB |

背景画像とカスタム魚の画像は **base64 のまま `userStatus` に埋め込まれ**、
クラウド保存では `userStatus` ごと1リクエストで送られる設計。
そのため画像を追加するほど送信サイズが増え、上限を超えると保存が丸ごと失敗する。

**なぜ画像が 2.41MB になったか**: 背景画像の縮小処理（`AquariumView.tsx`）が
**横幅しか制限していなかった**（`Math.min(MAX_WIDTH / img.width, 1)`）。
縦は無制限だったため、縦に長い画像がほぼ原寸のまま JPEG 化されていた。

### アカウントによって原因が違った（重要）

同じエラーでも、端末（アカウント）によって原因が別だった。

| アカウント | 背景画像 | カスタム魚の画像 | 主因 |
|---|---|---|---|
| iPhone側 | **3.02 MB** | 0.00 MB | 背景画像 |
| Android側 | 0.70 MB | **2.40 MB** | **カスタム魚の画像** |

最初は背景画像だけを圧縮したため、iPhone は復旧したが **Android は直らなかった**。
カスタム魚の画像も同じ経路で `userStatus` に入るため、両方を圧縮する必要があった。

### 修正内容

1. **画像圧縮の共通処理を新設**（`src/lib/image.ts`）
   - **背景画像**: 縦横**両方**を上限内に収めたうえで、1枚 250KB に収まるまで
     品質 → 寸法の順に落とす（水槽は最大10槽なので 250KB × 10 = 約2.5MB）
   - **カスタム魚の画像**: 魚は水槽の背景に重ねるため**透過が必須**で JPEG は使えない。
     透過を保ったまま小さくできる **WebP** を使い、1匹 60KB を上限にする
     （WebP 非対応の環境では PNG のまま寸法だけで詰めるフォールバックあり）
2. **アップロード時に適用**（`AquariumView.tsx` / `EncyclopediaView.tsx`）
3. **既存の画像も起動時に自動で圧縮**（`GameProvider.tsx`）。
   背景画像とカスタム魚の両方を対象に、上限超えを検出したら一度だけ圧縮し直して通知する。
   → **アプリを開き直すだけで、今ある大きい画像も自動で小さくなる。**
4. **送信前のサイズチェックと日本語の案内**（`sync.ts`）。
   4MB を超える場合は送信前に止め、「何が大きいのか（背景画像かカスタム魚か）」を
   具体的なMB数つきで伝える。413 が返った場合の文言も追加。

### 検証（ブラウザ実機）
- **背景画像**（1600×6000）: **3.40MB → 0.26MB（92%削減）**
- **カスタム魚の画像**（256px・透過あり）: **114KB(PNG) → 22KB(WebP)（81%削減）**
  かつ**透過が保たれている**ことを画素のアルファ値で確認
- 送信サイズの試算:
  - iPhone側 **3.05MB → 0.51MB**
  - Android側 **3.54MB → 2.61MB**（いずれも上限内）
- `npm run build` / `npx tsc --noEmit` / `npx eslint src` … すべて0件
- 実機でアプリが正常起動・エラーなしを確認（API疎通も正常）

### オーナーへ
**両方の端末でアプリを開き直せば、画像が自動で軽くなりクラウド保存が復旧します。**
「🗜️ 背景画像◯枚とカスタム魚◯匹の画像を軽くしました」という通知が出れば完了です。
画像の見た目は多少粗くなりますが、設定し直せば新しい圧縮処理で綺麗なまま軽く保存されます。

### 技術情報メモ
- 送信サイズは `user_status` が支配的。**画像を JSON に埋める設計自体は変えていない**ため、
  水槽（最大10槽）やカスタム魚（無制限）が増えれば将来また詰まりうる。
  根本解決するなら画像を別テーブル（`custom-fish` / `fish-overrides` と同じ方式）へ
  分離するのが筋。影響範囲が大きいため今回は見送った。
- 「クラウド保存が失敗する」と報告されたら、まず**本番ログでHTTPステータスを確認**すること
  （`npx vercel logs <デプロイURL>`）。413 なら画像肥大が原因。
- **同じ症状でも端末ごとに原因が違いうる**。今回は片方が背景画像、もう片方がカスタム魚だった。
  片方で直っても、もう片方で内訳を確認すること。

---

## 2026-08-28（追記13）: PR #17 マージ後の全体メンテナンス（データ消失バグ2件を修正）

PR #17 をマージして本番反映したうえで、オーナー依頼により最新コードで全体メンテナンスを実施。
「DB周りにエラーが多いので要注意」との指示を受け、同期（クラウド保存・復元）を重点的に精査した。

### 健全性チェック（すべて問題なし）

| 項目 | 結果 |
|------|------|
| 本番ビルド | ✅ エラー0件 |
| 型チェック（`npx tsc --noEmit`） | ✅ エラー0件 |
| ESLint | ✅ エラー0件・警告0件 |
| 脆弱性（`npm audit`） | ✅ **0件**（`browserslist` の1件を修正。ビルド時のみ使う開発用ツールで本番影響はなかった） |
| 実機疎通（`npm run dev`） | ✅ トップ200 / 認証200 / 同期401（未ログイン時の正常応答）/ 壊れたJSON400 |
| 本番デプロイ | ✅ PR #17 マージで自動デプロイ完了（Ready） |

### 修正1: 新しい端末で保存すると、クラウドのデータが警告なく消えるバグ（データ消失・重要）

**症状（再現条件）**
新しい端末（スマホ等）でログインし、**クイズを1回でも遊んでから**、
☁️復元をせずに 💾セーブ を押すと、**クラウドの単語・おさかな・穴抜け問題が警告もなく全部消える。**

**原因**
クラウドを守る仕組みが「未同期の新端末（`userStatus.lastUpdated === 0`）」に限定されていた。
ところがチュートリアル・設定変更・クイズなど、**何か1つでも操作すると `lastUpdated` に
時刻が入り、この保護の対象から外れる**。さらにクライアント側の警告も
「所持金0かつ学習回数0」が条件のため、1回でもGを稼ぐと素通りしていた。
結果、保護も警告も効かない隙間ができていた。

| 状況 | 修正前 | 修正後 |
|---|---|---|
| 新端末・何もせず保存 | ✅ 守られる | ✅ 守られる |
| 新端末・チュートリアル後に保存 | ⚠️ 警告は出るがOKすると消える | ✅ 確認画面で守られる |
| **新端末・1回遊んでから保存** | 🚨 **警告なしに消える** | ✅ 確認画面で守られる |
| 自分で全部消して保存 | ✅ 消える（意図どおり） | ✅ 確認後に消える |

**修正内容**
判定を「端末が未同期か」という**推測**から、「本人が意図的に空にしたのか」という**確認**に変えた。
手元が空でクラウドに残っている場合は既定で消さずにスキップし、
確認画面で了承を得た再送（`allowEmpty: true`）でのみ実際に削除する。
これにより「魚を全部ボックスに移した」「自分で全部消した」といった正当なケースも従来どおり動く。

### 修正2: 壊れたデータが1件でもあると、復元が丸ごと失敗するバグ

**症状**
クラウドのデータに壊れた行が1件でもあると、`JSON.parse` が例外を投げて
**復元API全体が500エラーになり、他の正常なデータも1件も戻せなくなる。**

**修正内容**
壊れた行だけを飛ばして、残りは必ず復元するようにした。
飛ばした件数は画面に通知する（黙って捨てない）。
オーナー方針「1つのエラーで全体を止めない・失敗は見える化する」に沿った実装。

### 変更ファイル
- `src/app/api/sync/push/route.ts`（空データ保護を確認ベースに変更）
- `src/app/api/sync/pull/route.ts`（壊れた行をスキップして復元継続）
- `src/lib/sync.ts`（`allowEmpty` の受け渡し、`brokenRows` の返却）
- `src/components/GameProvider.tsx`（`pushNow(allowEmpty)`、破損件数の通知）
- `src/components/Modals.tsx`（削除前の確認画面と再送）
- `package-lock.json`（`browserslist` の脆弱性修正）

### 積み残し（急ぎではない）

1. **獲得直後の魚に、獲得前の放置日数分のペナルティがかかりうる**（軽微）。
   放置ペナルティは全ての魚に一律適用されるため、日付の境目をまたぐと
   数時間しか飼っていない魚も1日ぶん引かれる。
   ※オーナー報告「獲得したばかりのジュゴンが病気になった」の**主因は別**で、
   ☁️復元時に `lastActiveTime` が巻き戻る問題だった（PR #17 の `mergeUserStatus` 修正で解決済み）。
2. **Next.js 16.2.9 → 16.3.3 の更新**。現在 `npm audit` は0件のため急がない。
3. **自動テストがリポジトリに無い**。同期まわりは壊れると被害が大きいので、
   `pushToCloud` / `pullFromCloud` の判定ロジックだけでもテストを置けると安全性が上がる。

---

## 2026-08-26（追記12）: マージ前の総点検で自分の回帰18件を発見・修正

未マージの6コミット（`7079fe5`〜`b7aa563`）を本番に出す前に、オーナー依頼で
「修正による悪影響がないか」を総点検した。同期系 / GameProvider / UI・ロジックの
3領域を並行レビューし、**18件**の問題を確認。うち致命的な4件はすべて
**今回の一連の修正で自分が作り込んだ回帰**だった。全件修正済み。

### 🔴 致命的（4件）

**1. 初期ロードで所持金・魚が表示されずデータを失う**
初期ロード effect は既存ユーザーの場合、`applyOfflineEffects` の副作用だけで
state をハイドレーションしていた。`fcbc387` で「経過1日未満なら早期 return」を
追加したため、**24時間以内に開くと所持金0・魚0匹で表示**され、その状態で操作すると
初期値が保存され、💾セーブでクラウドの userStatus まで潰れる状態だった。
ほぼ全ユーザーが初回起動で踏む。ハイドレーションとペナルティ適用の責務を分離して修正。

**2. `lastActiveTime` が壊れると自己修復しない**
`elapsedPenaltyDays` は `undefined`/`NaN` で NaN を返し、`NaN < 1` が false のため
早期 return をすり抜けて好感度と `lastActiveTime` を NaN に汚染していた。さらに
NaN は JSON 化で `null` になり、同期を経ると **`null` は数値演算で 0 に化けて
「約2万日経過」と解釈され、全魚が病気・逃走する**（テストで実測して発見）。
master は毎回 `now` を書いて自己修復していたが、その性質を失っていた。
入力を検査して 0 扱いにし、`applyOfflineEffects` 側でも壊れた値と未来時刻を `now` に修復。

**3. ガチャのG没収が条件を変えて残っていた（修正が不完全だった）**
`fcbc387` の事前チェックは「どれか1つでも空きのある水槽」を見るのに対し、受け取りは
「出た魚の水種の水槽1つ」しか宛先にしない。**ボックス満杯＋海水槽満杯＋淡水槽に空き
の状態で海水ガチャを回すと100%再現**した。宛先決定を1つの関数に寄せ、事前チェックと
受け取りで同じ判定を使うようにした。

**4. `/abort/i` の誤判定が未保存データの上書きを誘発**
mssql は `TransactionError: 'Transaction has been aborted.'` を投げ、push 全体は
トランザクションなので**SQLエラー全般がこの文言になる**。これをタイムアウト扱いして
「保存できているかもしれないので ☁️復元 で確認を」と案内していたが、実際は
ロールバック済みで、従うと未保存のローカルデータを失う。逆に本命の接続タイムアウト
`Failed to connect to ... in 30000ms` は "timeout" も "abort" も含まずマッチして
いなかった。タイムアウトだけを積極的に特定する形に修正。

### 🟠 中程度（10件）

- **同期ガードの設計をやり直した**: 「空配列なら消さない」判定が、
  ①魚を全部ボックスへ移すと復元時に**同じ魚が水槽とボックスに二重化** ②削除が他端末に
  伝播しない ③新端末の初期 `lastUpdated` が 0 なので `userStatusStale` が先に throw され
  **確認ダイアログに永久に到達しない**（機能が死んでいた）④「保存済みです」の文言が
  事実と逆、という4つの問題を生んでいた。判定を**「未同期の新端末か（lastUpdated===0）」**
  に変更し、`allowEmpty` と確認ダイアログ・再送を撤去。`pushNow` は throw をやめ戻り値に。
- **`resetPool()` を全エラーで呼んでいた**: body パース失敗・SQLエラー・**クエリ
  タイムアウト**でも健全なプールを破棄していた。コールドスタート対策と正面衝突し、
  失敗時ほど接続を張り直して無料枠を浪費する。また `close()` の await は使用中接続の
  返却を待つため応答が数十秒遅延しうる。接続系エラー限定にし、close は待たない形に。
- **魚の墓標3件**: `releaseBoxFish` だけ墓標が無く別端末経由で復活しうる／
  逃走時の墓標が `lastUpdated` 据え置きで push に載らない／`mergeUserStatus` の
  クラウド優先分岐が墓標を捨て、`lastActiveTime` の巻き戻しで**ペナルティが二重適用**。
- **ガチャ残り2件**: `addFishToBox` の戻り値を見ておらず支払い済みの魚が無言で消える／
  「もどる」で `nameInput` が残り次のガチャに前の名前が持ち越される（新規回帰）。

### 🟡 軽微（4件）
底生魚の移動範囲が反転して下端がはみ出す（特大・最大サイズ×背の低い水槽）／
文字列 `null` ボディが try の外で TypeError になり素の500／`tts` の変数シャドウイング／
存在確認の `COUNT(*)` を `TOP 1` に。

### 検証
- `npm ci` で lockfile と実体を揃えたうえで `npm run build` 成功 / `npx eslint` 0・0
- **純粋関数を実物のまま読み込んで21ケース実測**（テスト基盤が無いため
  `node --experimental-strip-types` ＋ 拡張子解決ローダ）。`Transaction has been aborted.`
  と `aborted` がタイムアウト扱いにならないこと、接続タイムアウトは拾うこと、
  `undefined`/`NaN`/`null`/未来時刻で好感度が壊れないこと、旧バグの再現と修正後の挙動を確認。
  **この実測中に `null` の穴（結果側の isFinite では捕まらない）を発見して追加修正した**
- dev サーバーで壊れたJSON→400、`null` ボディ→400、正常系と401が従来どおりを確認
- 項目1・3とガチャの網羅はコード検査で確認（React/UI の実行が必要なため）

### 🔴 オーナー作業（デプロイ後に最優先で確認）
**起動直後に所持金・おさかな・実績が正しく表示されること。** これが項目1の回帰で、
本番に出ていたらほぼ全ユーザーがデータを失っていた箇所。あわせて
「ボックスと水槽を満杯にして海水ガチャを回してもGが減らないこと」も確認してほしい。

---

## 2026-08-12（追記11）: `DROP USER` がスキーマ所有権で失敗していた（3回目の診断）

追記10 の手順（`DROP USER` → `CREATE USER ... WITH PASSWORD` → `ALTER ROLE`）を
オーナーが実行したが、**`Login failed for user 'aqualingua_user'` が再発**。

### 診断: 何が起きていたか

原因切り分けのため、次のクエリを実行してもらった。

```sql
SELECT name, type_desc, authentication_type_desc
FROM sys.database_principals
WHERE name = 'aqualingua_user';
```

結果は **`authentication_type_desc = INSTANCE`**（包含データベースユーザーなら
`DATABASE` になるはず）。つまり **`DROP USER` / `CREATE USER` が実際には効いておらず、
まだ古いサーバーログインに紐づいたまま**だった。

Microsoft Learn で裏を取ったところ、SQL Server / Azure SQL には
**「オブジェクトやスキーマを所有しているデータベースプリンシパルは `DROP USER` できない」**
という制約がある。`aqualingua_user` は長期間 `db_owner` としてアプリのテーブル運用を
してきたため、`dbo` スキーマ（またはそれに準じるスキーマ）の所有者になっていた可能性が高い。
この場合 `DROP USER` はエラーで失敗し、後続の `CREATE USER` も実行されない。
クエリエディタで複数行をまとめて流すと、エラーが出た行だけ埋もれて
「完了しました」のように見えることがあり、今回はそれで見落とされたとみられる。

⚠️ **追記10 では「スキーマを所有しているケースは通常出ない」と脚注扱いにしていたが、
実際に起きた。今回はこれを主因として扱う。**

### ✅ 正しい手順（1文ずつ実行し、各行の結果を確認しながら進める）

**① 何を所有しているか確認する**
```sql
SELECT s.name AS schema_name
FROM sys.schemas s
WHERE s.principal_id = USER_ID('aqualingua_user');
```
行が返れば、それが `DROP USER` を阻んでいるスキーマ（通常は `dbo` のみ）。

**② 所有権を `dbo` に移してから作り直す**（①で `dbo` 以外の名前も出た場合は、
その名前ぶんだけ同様に繰り返す）
```sql
ALTER AUTHORIZATION ON SCHEMA::dbo TO dbo;
DROP USER [aqualingua_user];
CREATE USER [aqualingua_user] WITH PASSWORD = '<新しい長いパスワード>';
ALTER ROLE db_owner ADD MEMBER [aqualingua_user];
```

**③ 直ったか再確認**
```sql
SELECT name, authentication_type_desc FROM sys.database_principals
WHERE name = 'aqualingua_user';
```
`authentication_type_desc` が `DATABASE` になっていれば成功。そのあと Vercel の
`AZURE_SQL_PASSWORD` を②の新しい値に更新し、**再デプロイ**する（追記10 の②③と同じ）。

### 🔀 フォールバック: `DROP USER` がさらに別のエラーで止まる場合

`aqualingua_user`（LOGINベース）はそのまま残し、**別名の新しい包含ユーザー**を作って
アプリの接続先を切り替える。`DROP` が不要なので所有権の問題を回避できる。

```sql
CREATE USER [aqualingua_app] WITH PASSWORD = '<新しい長いパスワード>';
ALTER ROLE db_owner ADD MEMBER [aqualingua_app];
```

Vercel の `AZURE_SQL_USER` を新しいユーザー名に、`AZURE_SQL_PASSWORD` も対応する値に
変更してから再デプロイする。

### コード変更は無し
今回のエラーは Azure Portal のクエリエディタ上で起きるものでアプリのAPIを経由しないため、
`friendlySyncErrorMessage` 等に追加すべき分岐は無い（`Login failed for user` の文言は
追記8 で案内済みの「①環境変数②再デプロイ③DB側が未変更」の③に含まれる）。

### 検証
- SQL 自体はこちらでは実行できない（Azure接続情報を持たない）。1文ずつ実行して
  各行の結果を報告してもらい、最終的に `authentication_type_desc = DATABASE` になった
  ことと、実機で ☁️復元 / 💾セーブ が通ることをオーナーに確認してもらう
- ドキュメントのみの変更のため `npm run build` / `npx eslint` に影響は無いが、
  習慣として実行し変わっていないことを確認した

---

## 2026-08-11（追記10）: パスワード手順の再訂正 ＋ 🔴 管理者アカウントの漏洩が判明

追記8 の手順をオーナーが実施したところ **SQL がエラーで実行できなかった**。
調べ直して原因が確定し、あわせて**より重大な漏洩**が見つかった。
**追記6・追記8 に書いた手順はどちらも誤り。この追記10 が正しい。**

### なぜ追記8 の手順で失敗したか

git 履歴の `create-db-user.js`（コミット `2a06c70`）を確認したところ、
アプリの接続ユーザーはこう作られていた。

```sql
-- master に対して
CREATE LOGIN [aqualingua_user] WITH PASSWORD = '...';
-- アプリDB（対象のデータベース）に対して
CREATE USER  [aqualingua_user] FOR LOGIN [aqualingua_user];
ALTER ROLE   [db_owner] ADD MEMBER [aqualingua_user];
```

つまり **`aqualingua_user` は包含データベースユーザーではなく、master のサーバーログイン**で、
**パスワードは LOGIN 側に紐づいている**。`FOR LOGIN` で作られたユーザーに
`ALTER USER ... WITH PASSWORD` は使えない（SQL Server が
「包含データベースユーザーのパスワードしか変更できない」と拒否する）。
追記8 で「包含データベースユーザーです」と断定したのが誤りだった。

`ALTER LOGIN` なら変更できるが、それは **master に接続して実行する必要がある**。
Azure Portal のクエリエディターはデータベース単位の接続で、`master` は一覧に出ないため
Portal だけでは辿り着けない。→ **master を触らずに済む方法に切り替える。**

### ✅ 正しい手順（これを使う）

**① DBユーザーを「包含データベースユーザー」に作り替える**
Azure Portal → **対象のデータベース**の画面 → **クエリエディター**に
**サーバー管理者**でログインして実行する（サーバーの画面ではなくDBの画面にある）。

```sql
DROP USER [aqualingua_user];
CREATE USER [aqualingua_user] WITH PASSWORD = '<新しい長いパスワード>';
ALTER ROLE db_owner ADD MEMBER [aqualingua_user];
```

- 漏れた元のパスワードとは違う、十分に長い新しい値にする
- パスワードに `'`（シングルクォート）を入れると SQL が壊れる。記号は `!` `#` `%` `-` 程度に
- これ以降パスワードはDB内で完結するので、**次回からは Portal だけで変更できる**
- アプリ側は接続時に `database` を明示しているため、包含ユーザーでもそのまま繋がる
  （`src/lib/azure-sql.ts` が `AZURE_SQL_DATABASE` を渡している）
- `DROP USER` が「スキーマを所有しているため削除できない」と出た場合のみ、
  先に `ALTER AUTHORIZATION ON SCHEMA::dbo TO [dbo];` を実行する
  ⚠️ **ここでは「通常は出ない」と書いたが、実際には出た。詳しい手順は追記11 を参照**
- master に残る古い `LOGIN [aqualingua_user]` は、対応するDBユーザーが無くなるので
  アプリDBには入れなくなる。将来 master に繋げる手段ができたら
  `DROP LOGIN [aqualingua_user];` で消すのが望ましい（任意・後回し可）

**② Vercel の環境変数を更新して再デプロイ**
1. `AZURE_SQL_PASSWORD` に ① で設定した値を入れる（**Production 環境**に入れること）
2. `AZURE_SQL_USER` = `aqualingua_user` /
   `AZURE_SQL_DATABASE`・`AZURE_SQL_SERVER` が従来どおりであることを確認
   （このリポジトリは公開なので、具体的な値はここに書かない）
3. **再デプロイする**（Deployments → 最新 → ⋯ → Redeploy）。
   環境変数はデプロイ時に焼き込まれるため、これをしないと古い値のまま

**③ うまくいったか確認**
アプリで ☁️復元 と 💾セーブ を1回ずつ。失敗したら画面の括弧内の文言を控える。
`Login failed` が続く → パスワード不一致か再デプロイ漏れ。
`Cannot open database` → ① の `ALTER ROLE`（権限付与）が漏れている。

### 🔴🔴 このリポジトリは公開（public）だった

GitHub API で確認したところ `umichuna/aqualingua-app` は **`"visibility": "public"`**。
つまり **漏れたパスワードは「履歴に残っている」だけでなく、インターネット上の誰でも
`git clone` して読める状態**にある。公開リポジトリは資格情報の自動収集の対象なので、
**すでに第三者に取得されている前提で考えるべき**。

これにより優先度が変わる。追記6〜9 では「履歴に残る」という表現に留めていたが、
実態は**公開露出**であり、ローテーションは「やったほうがよい」ではなく**急務**。

- リポジトリを private にすることも検討に値する（ただしフォークや各種キャッシュ、
  既に収集された分は取り消せないので、**private 化はローテーションの代わりにはならない**）
- 以後この報告書にも、サーバー名・DB名・アカウント名などの具体値は書かない方針にした
  （既に履歴にあるとはいえ、現行の追跡ファイルに集約すると発見されやすくなるため）

### 🔴 管理者アカウントのパスワードも漏れていた（新発見・最重要）

同じスクリプト群は、Azure SQL の管理者として
**オーナーの Microsoft アカウント（Entra ID）** に接続しており、
**そのパスワードも直書きのまま git 履歴に残っている**。
`aqualingua_user` のものと合わせて **実パスワードが2種類露出している**。

管理者アカウントは Azure サブスクリプション全体を操作し得るため、
DBユーザーより桁違いに重い。**追記6〜8 では「DBパスワード」しか扱っておらず、
こちらを完全に見落としていた。**

→ **オーナー作業: この Microsoft アカウントのパスワードを変更し、
可能なら多要素認証(MFA)を有効にする。** アプリの動作には影響しないので、
①②とは独立していつでも実施できる。履歴からは消えないため、
ローテーションが唯一の無効化手段。

対象ファイル（いずれも現在は git 追跡外だが、履歴には残る）:
`create-db-user.js` / `create-db-user-aad.js` / `setup_azure_user.js` / `setup_azure_sql.py`

### 直したこと（コード側）
- `friendlySyncErrorMessage` の `Login failed` 文言を、
  「①環境変数を確認 ②変更したら必ず再デプロイ ③それでも直らないならDB側が未変更」
  という切り分けの順序が分かる形に更新した
- README の環境変数の節に「変更したら再デプロイが必要」を明記した

### 検証
- `npm run build` 成功 / `npx eslint` error 0・warning 0
- `friendlySyncErrorMessage` のテスト（実エラー文字列を含む7ケース）が通ること
- ①②は Azure と Vercel の操作なのでオーナー実施待ち。**コード修正だけでは直らない**

### 反省（同じ失敗を繰り返さないために）
認証情報の作られ方を**確認せずに手順を書いた**ため、オーナーに2回無駄足を踏ませた。
今後この種の案内をするときは、先に作成スクリプトや履歴で
「どのアカウントか」「パスワードがどこに紐づくか」を確認してから書く。

---

## 2026-08-11（追記9）: 同期以外のバグ洗い出し（7件修正）

追記8 の同期修正のあと、オーナー依頼で同期以外も一通り点検した。7件見つけて全部直した。

### 1. 🔴 ガチャで水槽もボックスも満杯だと、Gを払った後に詰む

`ShopView` の容量チェックが**支払いの後**にあった。両方満杯だと
①`buyGachaFish()` でGは既に引かれている →②命名画面の「きめた！」で
「水槽もボックスも満杯！」と出て `return` →③**命名画面にキャンセルが無く、
背景タップでも閉じない**（設定モーダルには背景 `onClick` があるのにガチャには無かった）
→④リロードするしかなく、魚は消えてGだけ失う。

- ガチャを**回す前に**空きを確認するようにした。ボックスに空きがあればどの魚が出ても
  受け取れるので、「ボックス満杯 かつ どの水槽も満杯」のときだけ回させない
- 命名画面に「もどる」を追加。リビール画面に戻れば「にがす」が押せるので詰まない
- 万一の保険として、確定時に満杯だった場合もリビール画面に戻す

### 2. 🟠 実績報酬はボックス満杯を無視して入る（ガチャは弾くのに）

`addFishToBox()` が `boxCapacity` を一切見ておらず、上限を守っていたのは ShopView の
呼び出し側だけだった。そのため**実績報酬は上限を超えて入り、ガチャだけ弾かれる**非対称。
→ 上限判定を `addFishToBox()` に一元化し、戻り値 `boolean` で失敗を返すようにした。
**あわせて `claimAchievementReward` を修正**: 入れられなかったときに受取済みへ記録すると
報酬を永久に失うため、記録せずに「空きを作ってからGETしてね」と案内して終わる。

### 3. 🟠 約23時間ごとに開けば、好感度が永久に減らない

`applyOfflineEffects` が、経過1日未満で何も起きなかった場合でも
**`lastActiveTime` を無条件で `now` に進めていた**。しかもこれは `visibilitychange` の
たびに走る。結果、端数が毎回捨てられ「1日サボると-3」が実質機能していなかった。
→ `elapsedPenaltyDays()` を切り出し、**消化した日数ぶん（`日数 × DAY_MS`）だけ**進めて
端数を保持するようにした。1日未満のときは何もせず即 return（魚の一括書き込みも省ける）。

### 4. 🟠 逃げた魚・にがした魚に墓標が無い

単語には `deletedWordIds` があるのに魚には無く、魚の push は clear+rewrite なので
**別端末がまだ持っている魚を後から push すると復活**し得た。
→ `UserStatus.deletedFishIds` を追加。逃走・削除・カスタム魚削除の3経路で記録し、
pull 時に除外、`mergeUserStatus` で和集合を取る。上限1000は単語と共通化した。
魚IDは `crypto.randomUUID` で再利用されないため、単語のような「忘れる」処理は不要。
ボックスへの移動は削除ではない（`userStatus.boxFish` に移るだけ）ので対象外。

### 5. 🟡 3つのAPIで `await req.json()` が try の外だった

`translate` / `generate-examples` / `tts` は、壊れたJSONが来ると素の500になっていた
（`sync/push` は正しく try の中）。→ 3つとも `invalid JSON body` の400を返す。

### 6. 🟡 TTS の `rate` が NaN を素通り

`typeof NaN === "number"` なので検証を通過し、clamp してもNaNのままGoogleへ
`null` として送られていた。→ `Number.isFinite` で弾く。

### 7. 🟡 `sickStartTime: 0` だと逃走判定ごと飛ばされる（テストで発見）

`if (fish.isSick && fish.sickStartTime)` が真偽値判定だったため、`sickStartTime` が
`0`（epoch）のとき**逃走チェックに入らない**。`fish.sickStartTime || now` も同様に
0を潰す。実データでは `Date.now()` が入るので通常は起きないが、壊れた/古いデータで
踏み得る。→ `!= null` / `??` に変更。

### 調べて「問題なし」と確認したもの
- `finishQuiz` の引数順（PR #12 の修正は健在。`(mode, score, count)` で一致）
- 餌やりの空水槽ガード（呼び出し側の `displayFish` が同じ `resolveTankId` で絞っている）
- バックアップの13ストア網羅と旧形式バックアップの退避処理
- Service Worker のキャッシュ戦略（HTMLはネットワーク優先なので再デプロイは反映される）
- `parseInt` の基数漏れ・緩い等価比較（`==`）は全ソースに無し

### 検証
- `npm run build` 成功 / `npx eslint` error 0・warning 0
- `gameLogic` を実物のまま読み込んで12ケースのテスト。3の**旧実装を再現して
  「23時間ごとに10回開いても0日ぶんしか減らない」ことを確認**したうえで、
  修正後は9日ぶん適用されること、端数切り捨て・時計巻き戻し・病気/逃走判定を確認
  （このテストで7を発見した）
- dev サーバーで実際にAPIを叩き、壊れたJSON→400、`rate: null`→400、
  正常系は従来どおり進むことを確認
- 1・2・4はUI操作とDBが絡むためコード検査と型チェックのみ。実機確認は下記

### 🔴 オーナー作業（余裕のあるときに）
1. ガチャ: 水槽とボックスを満杯にして回そうとし、**Gが減らずに**
   「水槽もボックスも満杯！」と出ること。命名画面で「もどる」が効くこと
2. 実績: 満杯状態でGETを押しても受取済みにならず、空けてから再度GETできること
3. 魚の墓標: 端末Aで魚をにがす → 💾セーブ → 端末Bで ☁️復元 して復活しないこと

---

## 2026-08-11（追記8）: クラウド保存が `Login failed` で失敗する問題＋同期のバグ4件

### 何が起きたか

オーナー報告「クラウド保存に失敗する（パスワード更新済み）」「クラウドセーブできない」。
画面のエラーは **`Push failed: Login failed for user 'aqualingua_user'.`**

**原因は追記6・追記7 に私が書いたローテーション手順の誤り。**
アプリの接続ユーザー `AZURE_SQL_USER` = `aqualingua_user` は、`create-db-user.js` が
`CREATE USER ... WITH PASSWORD` で作った**包含データベースユーザー**で、
Azure ポータルの「パスワードのリセット」が変える**サーバー管理者ログインとは別のアカウント**。
にもかかわらず「ポータルでリセットして、その値を `AZURE_SQL_PASSWORD` に入れる」と
書いてしまったため、**サーバー管理者の新パスワードを `aqualingua_user` のユーザー名と
組で設定する**ことになり、当然一致せず認証エラーになった。

### ⛔ ここに書いた手順も誤りだった → **追記10 を参照**

この節には当初
`ALTER USER aqualingua_user WITH PASSWORD = ...` と書いていたが、
**`aqualingua_user` は master のサーバーログインとして作られていたため、この SQL は
実行してもエラーになる**（オーナーが実施してエラーを確認済み）。
正しい手順・原因は **追記10** に書き直した。この節の手順は使わないこと。

なお、次の1点は追記10 でも変わらず有効：
⚠️ **Vercel の環境変数は変更しただけでは動いている本番に反映されない。**
値はデプロイ時に焼き込まれるので、再デプロイしない限り古い値のまま。
「環境変数は直したのに直らない」の大半はこれ。

### 直した同期まわりのバグ（4件）

1. **接続プールが一度壊れると復旧しない**（`src/lib/azure-sql.ts`）
   `getPool()` が `pool.connected` だけを見ていた。mssql の `_connected` は一度 true に
   なると `close()` するまで false に戻らないため、パスワード変更・DBの自動一時停止・
   フェイルオーバーで実接続が張れなくなっても居座り続ける。実際に接続を作れているかは
   `healthy` が持っているのに見ていなかった。しかもクエリ失敗時にプールを捨てる処理が
   なかったので、**一度壊れるとそのインスタンスが生きている間ずっと同期が失敗し続けた**。
   → `healthy` も見て、壊れていたら `close()` してから作り直す。
   `resetPool()` を用意して pull/push 両ルートの `catch` からも呼ぶ
2. **手元が空のときクラウドを全消しする**（`src/app/api/sync/push/route.ts`。データ損失）
   `replaceTable()` が `DELETE` を無条件に実行してから「行が空なら return」していたため、
   `words`/`fish`/`blank_questions` が手元で空のまま 💾セーブ するとクラウドが消えた。
   `Modals` の「所持金0かつ学習回数0なら確認」の保険は、**少しでもGを稼いだ新しい端末では
   素通りする**。`userStatus` だけ新旧比較で守られている非対称も直した。
   → 空配列のときはクラウドの行数を数え、残っていれば**消さずにスキップ**して
   `skippedEmptyTables` で返す。クライアントは「クラウドも空にしますか？」と確認し、
   了承時だけ `allowEmpty: true` で再送する
3. **エラー文言が生の英語だった**（`friendlySyncErrorMessage`）
   無料枠切れしか日本語化しておらず、今回の `Login failed for user` すら英語のまま出ていた。
   → パスワード不一致（再デプロイの案内つき）・セッション切れ・タイムアウトを追加。
   タイムアウトは push/pull で文言を出し分ける
4. **タイムアウトを「失敗」と断定していた**
   クライアントは50秒で打ち切るが、Vercel の関数は止まらずコミットしうるため、
   **「保存失敗」と出たのに実際は保存されている**ことがある。
   → 「実際には保存できている場合があるので ☁️復元 で確かめて」という文言に変更

**変えなかったもの**: `src/lib/auth.ts` の `signIn` が DB 例外を握りつぶして `return true`
する点。ここで `false` を返すと DB障害時にアプリ自体が使えなくなる（ローカル第一の設計で
オフラインでも学習はできる）ため意図的に維持し、理由をコメントに明記した。
「ログインは通るのに保存だけ失敗する」の分かりにくさは 3 の文言改善で解消する。

### 検証
- `npm run build` 成功 / `npx eslint` error 0・warning 0
- `friendlySyncErrorMessage` を実物のまま読み込み、**実際に出たエラー文字列**
  （`Push failed: Login failed for user 'aqualingua_user'.`）を含む7ケースで日本語が
  返ること、push/pull の出し分け、未知のエラーは `null` のままを確認
- 2 の空配列ガードは**DBが必要なためコード検査と型チェックのみ**。
  実機での確認は下の「オーナー作業」に含める

### 🔴 オーナー作業
1. 上の「✅ 正しいパスワードローテーション手順」を実施する（**コード修正だけでは直らない**）
2. 直ったら ☁️復元 と 💾セーブ を1回ずつ試す
3. 余裕があれば 2 の確認: 単語が0件の状態で 💾セーブ し、
   「クラウドの単語は消さずに残しました」と出て、クラウド側が消えていないこと

---

## 2026-08-10（追記7）: AI翻訳・CSVのバグ修正（PR #15・#16）＋全体メンテナンス

### 直したバグ

**AI翻訳で「会話文」が文の途中で分割される（PR #15）**
- 症状: 種別「会話文」を ✨AI翻訳 すると1文として訳されず、断片ごとに別々の候補になる。
  例 `What is your go-to drink when you want to relax?` →
  「リラックスしたいとき」「あなたの定番のドリンクは何ですか？」の2候補に割れる
- 原因: `/api/translate` が入力の種別を見ず、常に Azure Translator の
  **辞書検索（`dictionary/lookup`）** を先に呼んでいた。これは単語・短い成句の言い換え候補用の
  APIで、長文を渡すと文中の語句を個別の辞書エントリとして解釈した断片訳を複数返してくる。
  「候補が0件のときだけ文翻訳にフォールバック」する実装だったため、
  （内容はおかしくても）候補が返る長文ケースでは正しい文翻訳に切り替わらなかった
- 修正: `wordType` をAPIに渡し、`会話文` のときは辞書検索を使わず文翻訳（`/translate`）だけを
  呼ぶ。結果もカンマ分割せず1文のまま返す（文中の読点で誤ってちぎれないように）。
  単語・述語・`wordType` 未指定の既存挙動は変更なし

**CSV・バックアップまわりのデータ損失ほか6件（PR #16）**
1. **CSV往復編集で「会話文」が「単語」に化ける（データ破損）**: `csv.ts` が種別列を
   「述語」以外すべて「単語」に丸めていたため、書き出して取り込む往復編集で分類が失われていた。
   3種すべて受け付けるようにした（空欄・未知の値は従来どおり「単語」）。
   種別は AI翻訳の方式や選択肢クイズの誤答選びの基準にもなるので、化けると実害が出る
2. **未登録ジャンルで「スキップ」を押すと既存単語が削除される（データ損失）**:
   スキップした行が削除候補の計算から外れ、アプリ側の既存単語がそのまま消えていた。
   `CsvImportResult` に `skippedIds` を追加し、スキップ行は「CSVに載っていた」ものとして
   削除判定から除外するようにした
3. **「📂 ロード」に確認ダイアログがなかった**: 全データ初期化やクラウドセーブには確認が
   あるのに、最も破壊的な全データ置き換えだけ即実行だった。セーブ日時と件数を見せて確認する
4. **既知ジャンルの判定が `customGenres` だけだった**: アプリ全体は `allGenres`
   （`customGenres` ＋既存単語のジャンル）を使うのに、CSV取り込みの判定だけ `customGenres` のみ。
   絞り込みに出ているジャンルなのに「未登録」と警告され、2の引き金にもなっていた。統一した
5. **`deletedWordIds` が蓄積されるだけで使われていなかった**: 削除済み単語の墓標に参照箇所が
   なく、別端末がまだ持っている単語を push した後に pull すると削除した単語が復活し得た。
   pull でローカル・クラウド両方の墓標の和集合を使って単語を除外し、成績も同時に除外
   （実在しない単語ぶん苦手件数が水増しされるのを防ぐ）。同じIDで再登録したら墓標から外す。
   `userStatus` は1件のJSONとして同期するため墓標に上限（1000）を設けた
6. **`generate-examples` の入力・出力検証**: `meanings` 未送信だと try の外で例外になっていた。
   生成結果の形も検証して、モデルが想定外の形を返しても画面に `undefined` が出ないようにした

### 全体メンテナンス（今回）

- **お掃除 / git 追跡の整理**:
  `.gitignore` に書いてあるのに**追跡され続けていた**ローカル作業用スクリプト6本
  （`verify-all-features.js`・`verify-changes.js`・`verify-debug.js`・`verify-sync-flow.js`・
  `debug-page-state.js`・`filter.js`）の追跡を解除した。
  **`.gitignore` はすでに追跡下にあるファイルには効かない**ため、前回追加した記述が
  働いていなかった。dev サーバーの実行時ファイル `dev.log`・`dev.pid` も追跡解除して
  `.gitignore` に追加。**ファイル自体は手元に残している**
  - 中身は精査済みで、**この6本に秘密情報は入っていなかった**（環境変数の「名前」に
    言及しているだけ）。前回の `create-db-user.js` などとは別件で、追加のローテーションは不要
- **依存の更新（範囲内のみ・オーナー判断）**:
  - 脆弱性 **high 1件 → 0件**。`nanoid 3.3.16 → 3.3.18`
    （サイズ0のとき無限ループし得る。`postcss` 経由の推移的依存）
  - `mssql 12.5.5→12.7.0` / `tailwindcss`・`@tailwindcss/postcss 4.3.0→4.3.3` /
    `playwright 1.60.0→1.62.1` / `papaparse 5.5.3→5.5.4` / `eslint 9.39.4→9.39.5` /
    `@types/react 19.2.17→19.2.18` / `@types/react-dom 19.2.3→19.2.4`
  - `package.json` の範囲指定は変更なし（`package-lock.json` だけ更新された）
  - **意図的に見送ったもの**（次回の判断材料）:
    `next 16.2.12 → 16.3.0` と `eslint-config-next 16.2.9 → 16.3.0` は、AGENTS.md にある
    「このリポジトリの Next.js は改変版で破壊的変更がある」という前提から、フレームワークの
    マイナー上げは影響確認を別作業に分けたほうが安全と判断した。
    `react`/`react-dom 19.2.4 → 19.2.8` も Next と足並みを揃えるため保留。
    `typescript 5→7`・`eslint 9→10`・`@types/node 20→26` はメジャーなので対象外
- **lint を error 6・warning 17 → 0・0**（挙動は変えていない）:
  - error 6件はすべて上記ローカルスクリプトの `require()` 記法。追跡解除しても手元には
    残るため、`eslint.config.mjs` の `globalIgnores` に加えて対象外にした
  - 未使用の変数2つを削除。`GameProvider` の `claimed`（`claimedAchievementRewards` を
    計算して未使用。参照が消えたことで「依存配列に足りない」警告も同時に解消）と、
    `StudyView` の `startBlankQuiz` 内の `blankWeakIds`（苦手優先ソートは
    `blankQuestionStats` を直接見ており未使用。UIの「苦手N問」表示に使う同名の変数は
    別スコープにあり、そちらは残している）
  - **効いていない `eslint-disable` を6件削除**（`GameProvider` 4件・`StudyView` 1件・
    `no-empty-function` 1件）。eslint 自身が
    「Unused eslint-disable directive（no problems were reported）」と報告していたもので、
    残すと将来の本物の警告を隠す。**まだ必要な抑制（`GameProvider` の3件など）は残した**
  - `gameLogic.ts` の `_jobLevel`・`_tankCapacity`・`_boxCapacity` は
    **呼び出し側が実引数を渡しているため signature から消せない**。`eslint.config.mjs` に
    `argsIgnorePattern: "^_"` を設定し、慣例どおり「意図的に未使用」として扱うようにした
- **README.md を書き直した**: `create-next-app` の雛形そのままで、このアプリの説明が
  一切なかった。画面一覧・技術構成・IndexedDB のストア・手動同期の方針・必要な環境変数
  （**名前だけ。値は書かない**）・ディレクトリ構成・Service Worker のキャッシュで
  「直しても反映されない」罠を記載した

### 🔴 未解決事項 / オーナー作業

1〜4は追記6 から**そのまま持ち越し**。今回のメンテナンスでは解消していない。

1. **DBパスワードのローテーション（必須・最優先／前回からの持ち越し）**
   2026-06-23 のコミット `2a06c70` で `create-db-user.js` などに直書きされた接続パスワードは
   **リポジトリの履歴に残っており、ファイルを消しても消えない**。
   → **正しい手順は追記10。** ここに以前書いていた
   「Azure ポータル → SQL Server → パスワードのリセット」も、
   追記8 に書いた `ALTER USER ...` も**どちらも誤り**なので使わないこと。
   さらに **`create-db-user.js` には管理者アカウントのパスワードも入っていた**
   （追記10 参照。こちらのほうが重大）。
   （**今回追跡解除した6本はこれとは別件で、秘密情報は含まれていない**。混同しないこと）
2. **クラウド同期の実機確認**: アプリで ☁️同期 と 💾セーブ を1回ずつ試す。
   失敗する場合はエラー文言を控える（`friendlySyncErrorMessage` が主な原因は日本語で知らせる）
3. **管理者パスワードのクライアント直書き（既知事項）**: 図鑑の編集ロックの合言葉は
   `EncyclopediaView.tsx` にそのまま書かれており、配信されたJSを見れば読める。
   誤操作防止用であり、ネットワーク越しに守るものではない
4. **8月の消費ペース次第で別DBへの移行を検討**（Supabase / Neon。
   テーブルが全て `userId + key + JSON` の単純構造なので移植は容易）
5. **`next 16.3.0` への更新**（今回の見送り分）。上げるときは
   `node_modules/next/dist/docs/` の非推奨記載を先に確認し、build と実機での描画・同期を通す

### 技術情報メモ（今回追加）
- **`.gitignore` に書いても、すでに git 追跡下にあるファイルは無視されない。**
  `git rm --cached <file>` で追跡を外す必要がある（今回の6本＋`dev.log`・`dev.pid` がこれ）
- lint の「Unused eslint-disable directive」警告は、抑制が不要になったサインとして使える。
  ただし**残っている抑制のうちどれが今も必要か**は eslint の出力で見分ける（消しすぎ防止）
- `eslint.config.mjs` に `argsIgnorePattern: "^_"` を入れたので、今後「引数を消せないが
  使わない」ケースは名前を `_` で始めれば警告にならない

---

## 2026-08-01（追記6）: 機能追加・バグ修正まとめ（PR #3〜#13）＋全体メンテナンス

7/09 以降この記録が止まっていたため、その間の対応をまとめて記載する。

### 追加した機能
- **海水ガチャ / 淡水ガチャ**（各700G・普通60/高級35/ロマン15）。その水種の魚だけが出る（PR #3）
- **おさかな図鑑の検索・絞り込み**: 名前検索＋レア度・水の種類・発見状況で絞り込み、件数表示とリセット（PR #5）
- **カスタム魚の種類名を 12 → 30文字**に拡大（長い魚名がフルネームで登録できなかったため。PR #6・#7）
- **カスタム魚の表示名を後から編集可能に**。図鑑のキー（type）は変えずに `displayName` を上書きする方式（PR #9）
- **水槽の上限を「各水種3槽」→「合計10槽・内訳自由」**に変更。海水・淡水の割合は任意（PR #8）

### 直したバグ
- **苦手判定の設定で「1」から他の数字に変えられない**（PR #4）
  数値入力をドロップダウンに変更。あわせて `<select>`/`<option>` の value が数値と文字列で
  食い違い選択が反映されていなかった問題も修正
- **図鑑の★が、その魚をボックスにしまうと消える**（PR #10）
  水槽の魚だけを見て最大レベルを判定していたため。`EncyclopediaEntry.maxLevelReachedAt` に
  到達を永続記録する方式に変更（発見済みと同じく一度到達したら消えない）
- **水槽の端で魚が小さくなる**（PR #11）。左右で別々の原因が同時に起きていた
  - 右端: Tailwind preflight の `img { max-width: 100% }` と絶対配置ラッパーの shrink-to-fit が
    噛み合い、画像が「右端までの残り幅」に収縮（水槽360pxで xlarge 128px → 57.6px を実測）
  - 左端: 位置が中心座標なのに移動範囲が魚のサイズ非考慮で、はみ出して見切れていた（実測 49.6px）
  - 併せて `tankId` 未設定の旧データの所属判定がコード間で食い違い、「淡水水槽に表示されるのに
    餌やりの対象外」になり得た問題を `resolveTankId()` に統一して修正
- **学習を「やめる」と表示と違う額しかもらえず記録も壊れる**（PR #12）
  `finishQuiz` の引数順が中断時だけ逆。10問3正解で中断すると表示100Gに対し実際30G、
  記録は「3問中10正解」になっていた
- **実績報酬の魚を二重に受け取れる**（PR #12）。受取済み判定が `setUser` の中にあり、
  魚の付与はその前に無条件で走っていたため、GETの二度押しで2匹もらえた
- **バックアップに組み込み魚の編集内容が含まれない**（PR #12）。`fishOverrides` /
  `sharedCustomFish` がバックアップ・全データ初期化の対象外だった
- **3000Gで買った水槽が同期で消えると復旧できない**（PR #12）。同期マージの救済対象に
  `tanks` を追加し、通帳の購入記録からの本数復旧も追加

### DB無料枠の追加対策（PR #13）
サーバーレスDBは「起きている時間」課金（自動一時停止までの待機時間も課金対象）のため、
クエリ本数より**起こす回数**を減らす方針で対応した。
- 7/5 に入れた「テーブル存在確認は1回だけ」のガードが **sync の2ルートにしか入っておらず**、
  `custom-fish` / `fish-overrides` は GET/POST/DELETE の計6箇所で毎回問い合わせていた → ガード追加
- 共有魚の自動取得を **6時間 → 24時間**に間引き。代わりに手動の ☁️同期 では間隔を無視して取り直す
  （pull で既にDBが起きているため追加コストはほぼ無い）
- `pull` を1バッチ化し、DBとの往復を **10回 → 1回**に（返却JSONの形は不変）
- 共有魚APIのリトライを **4回 → 2回**

### 全体メンテナンス（今回）
- **セキュリティ**:
  - `create-db-user.js`・`setup_azure_user.js`・`create-db-user-aad.js`・`setup_azure_sql.py` に
    **DB接続パスワードが直書きされたまま git 追跡下にあった**（2026-06-23 のコミット `2a06c70`）。
    追跡を解除し `.gitignore` に追加。ファイル自体は手元に残している
  - 依存の脆弱性を解消（**critical 1・high 3・moderate 1 → 0件**）。
    `next 16.2.9→16.2.12`、`next-auth 4.24.14→4.24.15`、`postcss`・`sharp` は overrides で更新
- **lint エラー 14件 → 0件**:
  - `BlankQuestionView` の `useEffect` が早期 return の後ろにあった（フック規則違反。将来クラッシュし得る）
  - 描画中の ref 参照を state に置き換え（`correctCount`、`ListenPlay` の単語リスト）
  - `WordManager` の「絞り込み変更で選択解除」を effect から描画中の前回値比較に変更
  - `page.tsx` の `?view=admin` 読み取りのみ、理由をコメントに書いた上で抑制（後述）
- **お掃除**: 未使用の `dailyGoldReward`・`buyTankSlot` を削除、
  `customFish.ts`/`fishOverrides.ts` に重複していた `fetchWithRetry`/`readError` を
  `src/lib/apiClient.ts` に集約、7月の調査用ログ `[SyncDebug]`（毎回の所持金ダンプ）を削除

---

### 🔴 未解決事項 / オーナー作業

1. **DBパスワードのローテーション（必須・最優先）**
   上記の認証情報は**リポジトリの履歴に残っており、ファイルを消しても消えない**。
   ⚠️ **ここに書いていた「Azure ポータル → SQL Server → パスワードのリセット」という
   手順は誤りだった。正しい手順は追記10 を参照すること**（この誤記が原因で
   `Login failed for user 'aqualingua_user'` が発生した）。
   （履歴の書き換えは既存クローン・PRへの影響が大きいため行っていない。
   ローテーションすれば漏れた値は無効になる）
2. **クラウド同期の実機確認**: 8/1 に無料枠がリセットされたはずなので、
   アプリで ☁️同期 と 💾セーブ を1回ずつ試す。失敗する場合はエラー文言を控える
   （`friendlySyncErrorMessage` が枠切れなら日本語で知らせる）
3. **管理者パスワードのクライアント直書き（既知事項）**: 図鑑の編集ロックの合言葉は
   `EncyclopediaView.tsx` にそのまま書かれており、配信されたJSを見れば読める。
   誤操作防止用であり、ネットワーク越しに守るものではない。
   本気で守るならサーバー側認証が必要（影響範囲が大きいため未対応）
4. **8月の消費ペース次第で別DBへの移行を検討**（Supabase / Neon。
   テーブルが全て `userId + key + JSON` の単純構造なので移植は容易）

### 技術情報メモ
- 共有魚の取得間隔は `GameProvider.tsx` の `CLOUD_FISH_REFRESH_INTERVAL_MS`（24時間）。
  localStorage キーは `cloudFishFetchedAt:*`。手動同期時は `cloudFishForceRefresh:*` を立てて
  1回だけ強制取得する（**取得日時は消さないこと**。消すと取得失敗時に間引きが解除され、
  以後アプリを開くたびにDBを起こしてしまう）
- `page.tsx` の `?view=admin` 読み取りは、`window` が事前描画時に存在せず state の初期値にできないため
  effect で読んでいる。`next/navigation` の `useSearchParams` は Suspense 境界が必要で
  静的事前描画の構成が変わるため使っていない（該当行のみ lint 抑制）
- `companions` ストアはアプリから読み書きしておらず常に空だが、既存のバックアップJSON・
  DBバージョンと結び付いているため削除していない

---

## 2026-07-05（追記5）: クラウド同期のデータ欠損バグ（調査中）＋拡張パック復旧

### 症状
一度クラウドにセーブしたデータの一部が、同期後に巻き戻る。
- 消費したエサ（items.baitBasic 等）が復活する
- 水槽拡張パック（tankCapacity）の購入が消える（お金は減り通帳にも記録が残るのに容量だけ元に戻る）

### 原因（サブエージェント調査で特定・確度高）
3つの要因が重なって発生：
1. `db.ts putUserStatus` は既存 `lastUpdated` を保持する（現在時刻に上書きしない）
2. 起動時のバックフィル処理が、クラウド由来の**古い lastUpdated のまま** userStatus を保存
3. `push/route.ts` の userStatus MERGE が LWW チェックなしの**無条件上書き**（コミット `ac39006` で"空セーブ対策"として意図的に削除された経緯あり）

→ 古い状態が新しい状態を上書きする。

### 今回の対応（オーナー判断）
- **同期の本修正は保留**し、まず**調査ログ**を仕込む（`sync.ts` の push/pull に `[SyncDebug]` ログ。何が上書きされ巻き戻るかを実データで確認してから直す）
- **拡張パック容量は復旧する**：通帳の「水槽拡張キット」購入回数×2＋初期4、「ボックス拡張キット」×5＋初期5 から本来値を計算し、現在値が下回っていたら起動時に戻す（`GameProvider.tsx` 初期ロード）
- バックフィル/復旧で userStatus を補正した際は `lastUpdated` を現在時刻に更新するよう修正（巻き戻り防止の一歩）

### 次回作業
- ユーザーの端末で `[SyncDebug]` ログ（willRollback:true が出るか）を確認
- 確認後に本修正（push の LWW 復活 or pull のガード）を方針決定して実装

### 変更ファイル
- `src/lib/sync.ts`（調査ログ）
- `src/components/GameProvider.tsx`（拡張パック復旧＋補正時 lastUpdated 更新）

---

## 2026-07-05（追記4）: 穴抜けクイズUI改善・書き出し機能

| # | 内容 | 変更ファイル |
|---|------|-------------|
| 1 | 単語帳の「書き出し」を絞り込み結果（`displayed`）のみ出力に修正 | `WordManager.tsx` |
| 2 | 穴抜け問題にCSV書き出し機能を追加（選択中ジャンルのみ出力） | `BlankQuestionView.tsx`, `csv.ts`（`blankQuestionsToCsv`追加） |
| 3 | 出題数入力で1の位（最後の桁）を削除できるよう空欄許可。空欄で開始したら警告してストップ | `StudyView.tsx` |
| 4 | 穴抜けクイズ: 正解時も解説を表示（従来は不正解時のみ） | `BlankQuestionView.tsx` |
| 5 | 穴抜けクイズ: 効果音を追加（正解=correct / 不正解=wrong） | `BlankQuestionView.tsx` |
| 6 | 選択肢を全文ではなく「単語だけ」表示（glow重なりで文字が消える不具合も解消） | `BlankQuestionView.tsx` |
| 7 | 穴抜けクイズ: 英文読み上げ（TTS）を追加。回答後に自動読み上げ＋🔊再生ボタン | `BlankQuestionView.tsx` |
| 8 | 「苦手問題のみ」→「苦手問題を優先」に変更（他クイズと同じ並べ替え方式） | `StudyView.tsx` |

---

## 2026-07-05（追記3）: 実績関連バグ修正・仕様変更まとめ

### 変更内容

| # | 内容 | 変更ファイル |
|---|------|-------------|
| 1 | 未受取の報酬魚をシルエット＆名前「？？？」に変更（受取後に正体が判明する演出） | `AchievementView.tsx` |
| 2 | 図鑑でLv.30（上限）到達済みの魚に⭐マークを表示 | `EncyclopediaView.tsx` |
| 3 | 実績「図鑑コンプリート」→「魚コレクター（30種類以上）」に変更 | `achievements.ts` |
| 4 | `lifetimeGoldEarned`が0の場合、通帳のプラス合計からバックフィル | `GameProvider.tsx` |
| 5 | `lifetimeWordsAnswered`が0の場合、カレンダー記録のcount合計からバックフィル | `GameProvider.tsx` |

### 変更理由

- **シルエット化**：報酬魚の正体が事前にわかると驚きが薄れるため
- **⭐マーク**：Lv.30はゲーム内最高レベル（`MAX_FISH_LEVEL = 30`）。図鑑から達成状況がわかるよう表示
- **魚コレクター**：図鑑コンプリートは報酬専用魚の存在・魚の追加で達成不可能になるため。30種類固定なら安定した目標になる
- **バックフィル**：`lifetimeGoldEarned` / `lifetimeWordsAnswered` は途中のバージョンで追加されたフィールドのため、それ以前のプレイ分が0のままだった。起動時に通帳・カレンダー記録から後追い補正する

### 技術情報メモ
- バックフィルは「フィールドが0かつ記録データが存在する場合」のみ実行（一度補正されると以後は実行しない）
- `lifetimeWordsAnswered`はfreeモード（count=0）を除外して合計する
- `lifetimeGoldEarned`は通帳のamount > 0（入金のみ）を合計する

---

## 2026-07-05（追記）: 実績報酬を手動GETボタン方式に変更

### 依頼内容
「水槽もBOXもいっぱいだと報酬を受け取れないのでは」という問題提起。
自動付与をやめて、実績画面の「🎁 GET」ボタンから任意のタイミングで受け取れるようにする。

### 変更内容

**GameProvider.tsx**
- 実績useEffectから自動付与ループを削除。解除検出と`unlockedAchievements`更新・通知のみに簡略化。
- `claimAchievementReward(achievementId)` 関数を新設。呼び出すと水槽に空きがあれば水槽、なければBOXへ魚を追加し`claimedAchievementRewards`に記録。
- 解除通知の文言を「実績画面でGETしてね！」に変更。

**AchievementView.tsx**
- 各実績カードに状態別の表示を追加：
  - 解除済み・未受取 → 「🎁 GET」ボタン
  - 解除済み・受取済み → 「受け取り済み ✅」テキスト
  - 未解除 → 魚プレビューのみ（GETもテキストもなし）

### 変更ファイル
- `src/components/GameProvider.tsx`
- `src/components/AchievementView.tsx`

### 検証（ブラウザ実機 2026-07-05）
- 型エラー0件（`npx tsc --noEmit`）
- 実績画面で「✅ 受け取り済み」が既受取実績に正しく表示されることを確認
- GETボタンのUI（コード・ロジック）は正常、報酬有り実績の未受取ケースで表示される

### 技術情報メモ
- `claimAchievementReward`は`fishRef`と`currentTankIdRef`（ref経由）を参照するため、useCallback依存配列はシンプル。
- BOXには容量上限なし（現仕様）。GETボタン方式により、水槽がいっぱいでも整理してから受け取り可能になった。

---

## 2026-07-05: WordManager・BlankQuestionView・StudyView・Modals 機能追加・デバッグ完了

### 依頼内容
以下5機能の実装とブラウザ実機での動作確認。

1. **単語帳 書き出しボタン**（📤 CSV書き出し）
2. **穴抜け問題 ジャンル管理**（CSVから自動取得）
3. **穴抜け問題 CSV取り込み**（papaparse）
4. **StudyView 穴抜けクイズ ジャンル絞り込みチップ**
5. **ホーム設定ラベル修正**（「セーブ・音・初期化」→「セーブ・音・アカウント」）

前日の実装セッションで追加した機能のうち、ブラウザ実機確認が残っていた項目を一括テスト・バグ修正。

### バグ修正内容

| バグ | 真因 | 修正 |
|------|------|------|
| 穴抜け問題のジャンルチップに「日常会話」「ビジネス」が表示されない | `BlankQuestionView`の`genres`が`game.allGenres`（単語帳側のジャンル）を参照していた | `blankQuestions`から動的に抽出するよう変更 |
| CSV取り込み時「行2: 〈〉がありません」エラーでインポートが0件になる | ヘッダー行フィルター（`r[0] !== "文"`）がBOM付きUTF-8で誤作動。最初のデータ行がヘッダーとして通過せず、全行がエラー扱いになっていた | フィルター・センテンス抽出ともにBOM(`﻿`)を除去してから判定するよう強化 |

### 動作確認内容（ブラウザ実機 2026-07-05）

| # | 確認項目 | 結果 |
|---|---------|------|
| ① | 単語帳「📤 書き出し」ボタン | `aqualingua-words-2026-07-05.csv`がダウンロード開始 ✅ |
| ② | 穴抜け問題のジャンルチップ（CSV取り込み後） | 「ビジネス」「日常会話」の2チップが表示 ✅ |
| ③ | 穴抜け問題CSV取り込み（papaparse） | 4件を取り込みました（エラー0件） ✅ |
| ④ | StudyViewのジャンル絞り込みチップ | 「ビジネス」選択→2問のみ表示 ✅ |
| ⑤ | ホーム設定ボタンの説明文 | 「セーブ・音・アカウント」に変更済み ✅ |

### 変更ファイル一覧
- `src/components/BlankQuestionView.tsx`（genres抽出元をblankQuestionsに変更、BOM処理追加）
- `src/app/page.tsx`（設定ボタンdesc修正・devbypass一時コード削除）

### 技術情報メモ
- 穴抜け問題のジャンルチップは、単語帳のジャンル（`game.allGenres`）とは完全に独立管理。穴抜け問題に登録された問題から自動で種類を抽出する。
- papaparse の `encoding: "UTF-8"` 指定時、BOM付きUTF-8ファイルでも先頭バイトが `r[0]` に残るケースがある。`replace(/^﻿/, "")` で確実に除去するのが安全。

---

## 2026-07-05: DB無料枠対策・引越しの水種制限・CSV文言変更（オーナー報告3件）

### 依頼内容
1. クラウド同期が「今月のデータベース無料枠を使い切った」エラーで失敗する
2. 海水魚が淡水水槽に引越しできてしまう（水の種類の制限がない）
3. 単語帳の「書き出し」ボタンを「編集用CSV」に文言変更

### 原因
1. **DB無料枠**: Azure SQL serverless の無料枠は月10万vCore秒・毎月1日リセット。
   最大の消費源は「アプリ起動のたびに魚の共有データを2本クラウド取得
   （各4回リトライ・55秒タイムアウト）」で、開くたびにDBが起こされ課金されていた。
   加えて同期のたびに穴抜けテーブルの存在確認クエリも毎回実行されていた。
   ※今月分の枠はコードでは復活しない。8/1の自動リセット待ち（オーナー了承済み）。
2. **引越し**: `moveFishToTank` に水の種類チェックがなかった（ボックス→水槽の
   `moveBoxFishToTank` には正しいチェックがあるのに、水槽間の引越しには未実装）。
   UI側も全水槽を引越し先として表示していた。
3. 文言のみ。

### 修正内容
1. DB無料枠対策（3点）:
   - 魚の共有データのクラウド取得を「起動のたび」→「**6時間に1回まで**」に間引き
     （localStorage にタイムスタンプ保存。ローカルキャッシュがあるので表示は正常。
     手動の☁️同期は従来通り即時）
   - 穴抜けテーブルの存在確認をサーバーインスタンスごとに1回だけに変更
   - 無料枠エラーを検知したら「今月のデータベース無料枠を使い切りました。毎月1日に
     自動リセットされるまで〜」という分かりやすい日本語で表示
     （`friendlySyncErrorMessage` を sync.ts に追加、syncNow と クラウドセーブの2箇所で使用）
2. 引越しの水種制限:
   - `moveFishToTank` に `moveBoxFishToTank` と同じガードを追加（不一致なら💧通知して中断）
   - 引越し先ボタンを「同じ水の種類の水槽」だけに絞り込み。該当水槽がなければ
     引越しセクション自体を非表示
3. 「📤 書き出し」→「📤 編集用CSV」

### 変更ファイル一覧
- `src/components/GameProvider.tsx`（水種ガード、6時間間引き、エラー文言）
- `src/components/AquariumView.tsx`（引越し先の絞り込み）
- `src/components/WordManager.tsx`（文言）
- `src/lib/sync.ts`（`friendlySyncErrorMessage` 追加）
- `src/components/Modals.tsx`（クラウドセーブ失敗時の無料枠メッセージ）
- `src/app/api/sync/push/route.ts`・`src/app/api/sync/pull/route.ts`（テーブル確認の1回化）

### 検証
- `npm run build` エラー0件・TypeScript通過
- `npm run dev` でアプリが正常に起動・描画されることを確認
- ログインが必要な画面（引越しUI・同期エラー表示）はオーナーの実機確認待ち

### 未解決事項 / 次回作業
- **8月1日に無料枠が自動リセット**されるまでクラウド同期は使えない（ローカル保存は正常）
- 8月の消費ペースを見て、それでも枠が厳しければ**別DBへの移行を検討**
  （候補: Supabase＝無料枠500MB・vCore秒課金なし / Neon＝Vercel統合が楽。
  本アプリのテーブルは全て「userId+key+JSON」の単純構造なので移植は容易、
  変更は接続ライブラリ＋API route 4本のみ）

### 技術情報メモ
- クラウド取得間引きの間隔: 6時間（`GameProvider.tsx` の `CLOUD_FISH_REFRESH_INTERVAL_MS`）。
  localStorage キーは `cloudFishFetchedAt:fishOverrides` / `cloudFishFetchedAt:customFish`。
  すぐに再取得したい場合はこのキーを削除すればよい
- DBを叩くAPIは4本のみ: `/api/custom-fish`・`/api/fish-overrides`・`/api/sync/pull`・`/api/sync/push`
  （auth/translate/tts はDB不使用）

---

## 2026-07-04（追記3）: 実績報酬魚9件の紐付け＋派生バグ2件を修正

### 依頼内容
実績10件中9件に、既存の魚（ジュゴン・シャチ・シーラカンス・オウムガイ・ホホジロザメ・
マンボウ・マンタ・リーフィーシードラゴン・ジンベエザメ）を報酬として割り当て。
「はじめての淡水デビュー」の1件は淡水魚を新規作成するまで保留（オーナー了承済み）。

### 対応1：報酬魚の紐付け
9匹とも既存の`rarity: "ロマン"`・`waterType: "saltwater"`の魚で、画像アセットも既存流用。
`fishMaster.ts`の各定義に`rewardOnly: true`と`linkedAchievementId: "<実績ID>"`を追加するのみ。
付与・重複防止ロジックは既に実装済みのため`GameProvider.tsx`側の変更は不要だった。
ロマン14匹中9匹を`rewardOnly`にしてガチャ対象外にしても5匹残るため、ガチャの排出プールは
枯渇しない（`rollGachaWithWeights`のフォールバックも確認済み）。

### 対応2：報酬が届かないバグ（真因＝開発環境でもService Workerが常時登録されていた）
オーナーから「達成済みなのに報酬が届かない」「他の実績も『報酬準備中』のまま」と報告。
コードロジック自体は正しかったが、`ServiceWorkerRegister.tsx`が**開発環境でも無条件に
Service Workerを登録**しており、`sw.js`のJS/CSSキャッシュ戦略がキャッシュ優先のため、
`fishMaster.ts`の修正が反映されないまま古いJSがブラウザに残り続けていたことが根本原因。
過去にも同種の「コード修正が反映されない」症状が繰り返し発生していた
（`feedback_aqualingua_service_worker_cache.md`参照）。

**恒久対策：**
- `src/components/ServiceWorkerRegister.tsx`：本番環境（`NODE_ENV === "production"`）でのみ
  Service Workerを登録。開発環境では既存の登録を自動解除し、Cache Storageも自動削除する
  ように変更（今後は手動でDevToolsから解除する必要がなくなる）
- `public/sw.js`：`CACHE_NAME`を`aqualingua-v3`→`v4`に更新。本番で既にキャッシュにハマって
  いるユーザーも、次回アクセス時に自動で新しいキャッシュへ切り替わる

### 対応3：報酬でもらった魚の名前バグ
オーナーから「報酬でもらった魚の名前が実績名（例：オリジナル飼育員）になっている」と報告。
実績付与処理（`GameProvider.tsx`）が魚の名前に実績のラベル文字列をそのまま渡していたのが原因。
通常のガチャ入手時（`ShopView.tsx`の`confirmName`）は種族名（`displayName`優先）をデフォルト名に
しているため、実績報酬もそれに合わせて`rewardFish.displayName ?? rewardFish.type`を渡すよう修正。

**既知の制限：** この修正より前に実績報酬で入手済みの魚（名前が実績ラベルのまま）は
自動では直らない。水槽の改名機能で手動修正が必要（オーナーへ伝達済み、対応不要と回答あり）。

### 変更ファイル一覧
- `src/data/fishMaster.ts`（9匹に`rewardOnly`/`linkedAchievementId`追加）
- `src/components/ServiceWorkerRegister.tsx`（本番のみSW登録、開発環境は自動解除）
- `public/sw.js`（`CACHE_NAME`を`v3`→`v4`）
- `src/components/GameProvider.tsx`（報酬魚の名前を種族名ベースに修正）

### 検証
- `npm run build`：3回とも都度エラー0件・TypeScript通過を確認
- ブラウザ実機（オーナー確認）：ハードリロード後、実績報酬（リーフィーシードラゴン等）が
  正しく届くことを確認。魚の名前が種族名になることは次回付与分から反映（ビルド確認済み、
  実機の最終確認はオーナー側で継続）

### 未解決事項 / 次回作業
- 「はじめての淡水デビュー」実績：淡水魚を新規作成した時点で`linkedAchievementId`を紐付ける
- 修正前に付与済みの魚（名前が実績ラベルのまま）の一括改名は不要と回答済み（対応不要）

### 技術情報メモ
- Service Workerは今後、本番デプロイのたびに`sw.js`の`CACHE_NAME`を上げる運用を継続する
  （新しいコードを確実にユーザー端末へ反映させるため）
- 開発環境（`npm run dev`）でコード修正が反映されない症状が今後出た場合、今回の修正で
  Service Worker起因の可能性は排除されているはずなので、別原因（Next.jsの`.next`キャッシュ等）
  を先に疑うこと

---

## 2026-07-04（追記2）: 水槽を開いた直後に魚が絵文字/古い画像になる問題を修正

### 症状（オーナー報告）
水槽を開いた直後の数十秒間、魚が絵文字（🐟）や古いイラストで表示され、しばらくして直る。

### 原因
魚の正しい画像は「組み込み魚の編集内容(fishOverrides)」と「共有カスタム魚(sharedCustomFish)」から
組み立てられ、これらは起動時にクラウド(Azure SQL)から取得している。ところが：
- **fishOverrides**: クラウド取得分を**ローカルに保存し直していなかった**（state更新のみ）。
- **sharedCustomFish**: **ローカルキャッシュが一切なく毎回クラウドからのみ取得**。他ユーザー作の
  カスタム魚はローカルに無いため取得完了まで絵文字になる。
- Azure SQL はコールドスタートで数十秒かかることがあり、その間ずっと未取得＝誤画像。
  表示開始判定(fishDataReady)も「クラウド取得 or 3秒タイムアウト」で、3秒で見切り発車していた。

### 修正内容
クラウド取得結果をローカル(IndexedDB)にキャッシュし、次回以降は起動時に即座に正しい画像を出す：
- DBに `sharedCustomFish` ストアを新設（db.ts、DB_VERSION 6→7。既存データは保持）。
- 起動時にローカルキャッシュ（fishOverrides / sharedCustomFish）を即読込→反映。
- クラウド取得成功時に `putFishOverridesBulk` / `replaceSharedCustomFish` でローカルへ保存。
- 表示開始判定を「クラウド待ち」から**「ローカル読込完了」**に変更（クラウドは裏で更新）。
  3秒タイムアウトの保険は維持。ローカル読込失敗時も固まらないよう .catch/.finally を追加。

### 変更ファイル
- `src/lib/db.ts`（sharedCustomFishストア追加、putFishOverridesBulk / getAllSharedCustomFish /
  replaceSharedCustomFish 追加、DB_VERSION 7）
- `src/components/GameProvider.tsx`（ローカルキャッシュ読込・保存、fishDataReady判定変更）

### 検証（ブラウザ実機）
- DBが v7 へマイグレーション成功、`sharedCustomFish` ストア作成、既存データ保持を確認。
- 新しい起動判定でアプリ正常ロード、魚も表示、自コード由来のエラー0件。`npm run build` 0エラー。
- ※クラウド→キャッシュ→次回即表示の完全な流れは要ログインのため実機未再現だが、機構は単純
  （IndexedDB読み書き）でビルド・マイグレーション確認済み。**初回だけはキャッシュが無いので
  従来通り取得待ちの窓が出るが、2回目以降は即座に正しい画像**。

### 注意（PWAキャッシュ）
本修正はコード側。ユーザー端末では Service Worker が更新されるまで旧版が残りうる。
本番反映後は一度アプリを開き直すこと。

---

## 2026-07-04（追記）: 既に解除済みの実績に報酬魚が届かない問題を修正

### 症状（オーナー報告）
公開後に確認したところ、達成済み実績（✅チェック）に対して報酬の魚が見当たらない。

### 原因
報酬魚は「実績がロック→解除された**瞬間**」にしか付与していなかった。機能公開時、
既存の達成は「後追い」でまとめて解除されたが、その時点では報酬魚がまだ登録されて
いなかったため魚なしで解除だけ完了。後から図鑑で報酬魚を紐付けても、その実績は既に
`unlockedAchievements` に入っており `checkNewAchievements` の対象外→二度と付与されなかった。

### 修正内容
「解除済み」と「報酬受け取り済み」を分離管理：
- `UserStatus.claimedAchievementRewards?: string[]` を追加（types.ts）
- 実績effect（GameProvider.tsx）を拡張：解除済み（既存＋新規）の実績のうち、報酬魚が
  登録済みで **まだ受け取っていない**（claimedに無い）ものを検出して後追い付与。
  付与時に claimed に記録し二重付与を防止。水槽ごとの容量判定はそのまま（B5）。
- 通知文言も「実績解除」と「実績報酬」を分離。

### 変更ファイル
- `src/lib/types.ts`（claimedAchievementRewards 追加）
- `src/components/GameProvider.tsx`（実績effect拡張、ACHIEVEMENTS import追加）

### 検証（ブラウザ実機）
- 解除済み「オリジナル飼育員」に後から報酬魚を紐付け → 次回ロードで魚が水槽に付与
  （1匹→2匹）、`claimedAchievementRewards=["custom_fish_creator"]` 記録、再ロードで
  二重付与なしを IndexedDB とホーム画面で確認。`npm run build` エラー0件。

### 重要メモ（次回・オーナー向け）
- **PWAのService Workerが古いJSをキャッシュする**（キャッシュ名 `aqualingua-v3`）。
  今回もこれが原因でローカル検証時に修正が反映されず、SW解除＋Cache削除で解決した。
  本番反映後、ユーザー端末では新SWが有効になるまで（またはアプリ再訪／再起動まで）
  旧版が表示されうる。届かない場合は一度アプリを開き直す必要がある。
- **報酬魚が「紐付け済み」でない実績には、当然ながら魚は届かない**（設計通り）。
  図鑑のカスタム魚追加で「🎁 実績専用にする」→対象実績を選んで登録すると、
  その後のロードで後追い付与される。

---

## 2026-07-04: 実績システム バグ一括修正（7件）

### 依頼内容
実績リワード機能の実装後、「複数の実績を同時に解除したときに報酬魚が消えないか」等の
疑問をきっかけにコード監査を実施。`GameProvider.tsx` の実績付与まわりで確定した7件の
バグ（B1〜B7）をまとめて修正。

### 原因（確定バグ）
| # | 場所 | 不具合 |
|---|------|--------|
| B1 | 実績effect | `encyclopediaCount` が実績専用魚を含む `encyclopedia.length`。図鑑コンプリートが誤って早期解除 |
| B2 | 実績effect | `setUser` の更新関数**内**で `addFishToTank`/`pushNotice` 等の別state更新を呼んでいた |
| B3 | `addFishToTank` | `setFishList([...fishRef.current, fish])` の絶対値更新で、複数付与時に最後の1匹しか残らない |
| B4 | `addFishToBox` | `persistUser(userRef基準)` の絶対値更新で boxFish消失＋`unlockedAchievements` 巻き戻し |
| B5 | 実績effect | 容量チェックが全水槽合計。正しくは現在の水槽内の匹数で判定すべき |
| B6 | 後追い専用effect | 通常effectと重複発火の温床 |
| B7 | 実績effect | `ready`/`fishDataReady` を待たず評価し、カスタム魚未ロードで図鑑コンプリート誤解除 |

### 修正内容
- `addFishToTank`：関数型更新 `setFishList((list)=>[...list, fish])` に変更（B3）
- `addFishToBox`：関数型 `setUser` + `putUserStatus`（updater内・冪等）に変更（B4）
- 実績effectを1本に統合。後追い専用effectと `hasCheckedRetroactiveAchievements` ref を削除：
  - 冒頭 `if (!ready || !fishDataReady) return;` ＋ deps に追加（B7）
  - `encyclopediaCount` を実績専用魚除外に統一（B1）
  - `grantedRewardRef`(Set) で同期的に二重付与ロック（B2/B6）
  - 新規解除IDは1回の関数型 `setUser` でまとめて追記、副作用ゼロ（B2）
  - 魚付与・通知は updater の外でループ。現在の水槽内の匹数をローカル追跡し容量判定（B5）

### 変更ファイル一覧
- `src/components/GameProvider.tsx`（3箇所）
- ※ `AchievementView.tsx` / `achievements.ts` は既に正しく変更なし

### 検証
- `npm run build`：エラー0件・TypeScript通過を確認済み
- **ブラウザ実機確認済み（2026-07-04）**：ローカルのみのログイン迂回（`?devbypass=1`、確認後に削除済み）で
  実績画面を操作。以下を確認：
  - 実績タブが全10件正常表示、進捗表示も正しい
  - カスタム魚を1匹追加→「オリジナル飼育員」が✅解除、**リロード後も保持**（永続化・B4系OK）
  - カスタム魚追加後も「図鑑コンプリート」は🔒のまま＝**誤解除なし（B1/B7の統一ロジックが機能）**
  - Console に「Cannot update a component while rendering」警告なし（B2）。
    ※出た401エラーは迂回ログインによる無関係なもの。
- **未実施（コード＋ビルドで担保）**：複数の報酬魚を同時付与するB3/B5の実操作再現は、
  実績に紐付いた報酬魚を事前登録した状態が必要なため今回のまっさら環境では未再現。
  ロジックはコードレビュー＋ビルドで確認済み。

### 技術情報メモ
- `fishDataReady` は 3秒タイムアウトで強制true化される（遅い回線対策）。ゲートはそれと併用。
- `grantedRewardRef` はセッション内の同期ガード。永続的な真実は `user.unlockedAchievements`。
  リロード時は Set が空でも `checkNewAchievements` が解除済みを除外するため二重付与しない。

---

## 2026-07-03: クライアント修正指示5件対応

### 依頼内容
1. 水槽切り替えボタンを背景ボタンの左隣へ配置
2. 水槽の独立管理（餌・収容数の水槽ごとの分離）
3. 水槽内の泡エフェクト削除
4. START画面→水槽画面 切り替え直後の魚画像バグ修正
5. 状況報告書の運用開始（このファイル）

### 原因

| # | 原因 |
|---|------|
| 1 | 水槽切り替えボタン（🔄）と背景ボタン（🖼️）がそれぞれ独立した`position: absolute`（左右上下を個別指定して画面上に固定表示する配置方法）で別の場所に置かれていた |
| 2a | 餌やり処理（`feedAllFish`）が「今見ている水槽」で絞り込まず、口座内の全水槽の魚に一律で効果を適用していた |
| 2b | 水槽の収容数上限が2種類の値で二重管理されていた。`Tank.capacity`（水槽ごとの値、常に50固定で実質未使用）と`UserStatus.tankCapacity`（口座全体で共有する値、初期4→ショップで+2ずつ）。実際の判定（ガチャ確定時など）は「口座全体の匹数」と「口座全体の上限」を比較しており、水槽Aが満杯でも水槽Bが空いていればガチャで貰えない、逆に表示と実態がズレる、といった不整合があった |
| 3 | 泡エフェクトは5個の固定位置div（画面の決まった位置に描かれる四角い領域の単位）をアニメーションさせているだけの装飾コードだった |
| 4 | アプリ起動時、ローカル保存データの読み込みが終わった時点で「準備完了」フラグ（`ready`）が立ち、水槽画面の描画が始まっていた。しかし「編集済みの魚の見た目」「みんなで共有しているカスタム魚」のクラウドからの取得は別処理で非同期に進んでおり、これが終わる前に一瞬だけ水槽が描画されてしまうことがあった（回線が遅い/サーバーが休止から目覚める途中だと特に発生しやすい） |

### 修正内容

| # | 内容 | 主な変更ファイル |
|---|------|----------------|
| 1 | 🔄水槽切り替えボタンと🖼️背景ボタンを1つの横並び（flexコンテナ、要素を横に並べるレイアウト手法）にまとめ、画面右下に隣接配置 | AquariumView.tsx |
| 2a | `feedAllFish`に「今表示中の水槽の魚だけ」に絞り込む条件を追加 | GameProvider.tsx |
| 2b | 水槽ごとの収容数は「上限＝口座共通の`tankCapacity`」「現在数＝その水槽にいる魚だけを数える」に統一。`Tank.capacity`（50固定だった不要な値）は型から実質廃止（後方互換のため型定義は残すが読み書きしない） | GameProvider.tsx, ShopView.tsx, types.ts |
| 3 | 泡エフェクトのコードを削除 | AquariumView.tsx |
| 4 | 「編集済み魚データ」「共有カスタム魚データ」の初回取得が終わるまで水槽描画を待つ`fishDataReady`という新しい待機フラグを追加。回線が遅くても3秒でタイムアウトして表示を進める（読み込み画面が固まらないようにする保険） | GameProvider.tsx, page.tsx |
| 5 | この状況報告書の運用を開始 | STATUS_REPORT.md（新規） |

### 変更ファイル一覧
- `src/components/AquariumView.tsx`（ボタン配置・泡削除）
- `src/components/GameProvider.tsx`（餌やり水槽絞り込み、収容数統一、`fishDataReady`ゲート追加）
- `src/components/ShopView.tsx`（収容数チェック・表示を現在の水槽基準に統一）
- `src/app/page.tsx`（`fishDataReady`を読み込みゲートに追加）
- `src/lib/types.ts`（`Tank.capacity`を非推奨化、`UserStatus.backgroundImageBase64`という使われていない古いフィールドを削除）
- `STATUS_REPORT.md`（新規、このファイル）

### 動作確認内容（実施済み）
ローカル環境（`npm run dev`）をブラウザで直接操作して確認：
- `npm run build` でエラー0件
- 🔄水槽切り替えボタンと🖼️背景ボタンが画面右下に隣接して並んでいることを座標で確認
- 泡の自動アニメーション要素がDOM上に存在しないことを確認
- 水槽1（海水1）で魚Aに餌やり→好感度 10→15、同時刻の水槽2（海水2）の魚Bは10のまま変化なし（水槽ごとの独立を確認）
- ショップのガチャ画面の「水槽 ○/4」表示が、今見ている水槽の匹数だけを反映することを確認（水槽を切り替えると表示も切り替わる）
- START画面→水槽画面の遷移直後から、組み込み魚・ガチャ産魚とも正しい画像で表示され、旧画像や🐟プレースホルダーの一瞬表示は発生しないことを確認

### 未解決事項 / 次回作業
特になし。依頼された5項目はすべて対応・確認済み。

### 技術情報メモ
- `fishDataReady`のタイムアウト値：3秒（`GameProvider.tsx`内の該当useEffect）。回線状況次第で調整可
- 水槽の収容数上限（`UserStatus.tankCapacity`）：初期値4、ショップの「水槽拡張キット」で+2ずつ、上限15（`MAX_TANK_CAPACITY`）
- 水槽は海水・淡水それぞれ最大3槽（合計最大6槽）
- **重要な開発時の注意点**：このアプリはPWA（ホーム画面に追加してアプリのように使える仕組み）としてService Worker（バックグラウンドでキャッシュ管理を行う仕組み）を使っている。ローカル動作確認中、コードを直しても**ブラウザが古いキャッシュを表示し続ける**現象が発生した（`npm run dev`を再起動しても直らない）。原因はブラウザ側に登録済みのService Worker（`sw.js`、キャッシュ名`aqualingua-v3`）が新しいコードへのアクセスをブロックしていたため。開発中に「直したのに反映されない」場合は、ブラウザの開発者ツールでService Workerの登録解除＋キャッシュ削除、またはシークレットウィンドウでの確認を先に試すこと
